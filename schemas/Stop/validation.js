import * as yup from 'yup';

/* * */
/* DOCUMENT TYPE: STOP */
/* Explanation needed. */
/* * */

/* * */
/* A. YUP Validation Schema */
export const Validation = yup.object({});
/*
  // General
  code: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  name: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  short_name: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  tts_name: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  latitude: yup.number().default(0).required(),
  longitude: yup.number().default(0).required(),
  location_type: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  platform_code: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  parent_station: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  url: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  public_visible: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),

  // Operation
  agencies: yup.array(),

  // Administrative
  address: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  postal_code: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  jurisdiction: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  region: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  district: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  municipality: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  parish: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  locality: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  stepp_id: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),

  // Infrastructure
  has_pole: yup.number().required(),
  pole_material: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_shelter: yup.number().required(),
  shelter_code: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  shelter_maintainer: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_mupi: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_bench: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_trash_bin: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_lighting: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_electricity: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  docking_bay_type: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_infrastructure_maintenance: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_infrastructure_check: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),

  // Public Information
  has_stop_sign: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  stop_sign_maintainer: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_pole_frame: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  shelter_frame_area_cm: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_pip_real_time: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  pip_real_time_code: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_h2oa_signage: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_schedules: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_network_map: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_schedules_maintenance: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_schedules_check: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_stop_sign_maintenance: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_stop_sign_check: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),

  // Accessibility
  has_sidewalk: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  sidewalk_type: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_tactile_schedules: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  stop_access_type: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_crosswalk: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_tactile_pavement: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_abusive_parking: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  has_audio_stop_info: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  wheelchair_boarding: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),
  last_accessibility_check: yup.string().max(100, 'Field_ deve ter apenas ${max} caracteres.'),

  // Services
  near_health_clinic: yup.boolean(),
  near_hospital: yup.boolean(),
  near_university: yup.boolean(),
  near_school: yup.boolean(),
  near_police_station: yup.boolean(),
  near_fire_station: yup.boolean(),
  near_shopping: yup.boolean(),
  near_historic_building: yup.boolean(),
  near_transit_office: yup.boolean(),

  // Intermodal Connections
  light_rail: yup.boolean(),
  subway: yup.boolean(),
  train: yup.boolean(),
  boat: yup.boolean(),
  airport: yup.boolean(),
  bike_sharing: yup.boolean(),
  bike_parking: yup.boolean(),
  car_parking: yup.boolean(),

  // Notes
  notes: yup.string().max(10000, 'Field_ deve ter apenas ${max} caracteres.'),

  //
});
*/
