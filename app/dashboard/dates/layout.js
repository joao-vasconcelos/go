'use client';

import OneFullColumn from '../../../layouts/OneFullColumn';

export default function Layout({ children }) {
  //

  //
  // A. Render components

  return <OneFullColumn first={children} />;
}
